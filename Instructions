To apply and commit in one step:

git am < geoserver-json-sqli-fix.patch


Using the patch command:
If you are not using Git, you can use the standard Unix patch utility from the root of your source directory:

patch -p1 < geoserver-json-sqli-fix.patch


Once applied, you will need to recompile the affected modules and redeploy your GeoServer instance.

This patch is written in the **Unified Diff format** (specifically, a Git-formatted patch). This is the standard output generated by commands like `git format-patch` or `git diff`, and it is typically saved with a `.patch` or `.diff` file extension. It shows the exact lines to be removed (marked with `-`) and added (marked with `+`), along with metadata about the commit.

Here is how you would structure this as a GitHub repository, complete with the `README.md`, an About section, a Disclaimer, and the patch file itself.

### Repository Structure

* `README.md`
* `geoserver-json-sqli-fix.patch`

---

### `README.md`

```markdown
# GeoServer `jsonArrayContains` SQL Injection Patch

## About
This repository contains an emergency security patch authored by **UBITQUITY** for an unauthenticated SQL injection vulnerability discovered in GeoServer (August 2026). 

The vulnerability exists within the `jsonArrayContains` function when utilized alongside PostGIS and Oracle DataStores. Due to improper sanitization of user-supplied parameters, attackers could break out of the SQL query context and execute arbitrary SQL commands. On certain configurations (e.g., running with administrator privileges), this can lead to Remote Code Execution (RCE). 

This patch mitigates the vulnerability by applying the `escapeLiteral()` method to the evaluated string parameters before they are concatenated into the final SQL output, neutralizing the injection vector.

## Disclaimer
**SECURITY AND LIABILITY WARNING**
This patch is provided "AS-IS" without warranties or guarantees of any kind, either expressed or implied. 
*   This code is released for educational and defensive purposes to assist administrators in securing their infrastructure until an official upstream release is available.
*   Applying unofficial patches can have unintended side effects on your production environment. 
*   **UBITQUITY** and the authors of this patch assume no liability for any system instability, data loss, or security incidents resulting from the application or misuse of this code. 
*   Always test patches in a staging or development environment before deploying to production.

## How to Apply the Patch

You can apply this patch to your local GeoTools/GeoServer source code repository using standard command-line tools.

**Using Git:**
If you are inside a Git repository, run:
```bash
git apply geoserver-json-sqli-fix.patch

```

*To apply and commit in one step:*

```bash
git am < geoserver-json-sqli-fix.patch

```

**Using the `patch` command:**
If you are not using Git, you can use the standard Unix `patch` utility from the root of your source directory:

```bash
patch -p1 < geoserver-json-sqli-fix.patch

```

Once applied, you will need to recompile the affected modules and redeploy your GeoServer instance.

```

---

### `geoserver-json-sqli-fix.patch`

```diff
From 84b3e821b0e34a65b90f421e67c8df4f5ab61d9a Mon Sep 17 00:00:00 2001
From: UBITQUITY <ubitquity@example.com>
Date: Thu, 13 Aug 2026 21:33:48 -0400
Subject: [PATCH] Security: Fix SQL injection vulnerability in jsonArrayContains function

This patch addresses an unauthenticated SQL injection vulnerability in 
the `jsonArrayContains` function when used with PostGIS and Oracle DataStores.
The flaw permitted remote attackers to execute arbitrary SQL commands 
because string parameters were not properly escaped before being 
concatenated into the generated SQL query.

This commit sanitizes the input parameters by applying `escapeLiteral` 
to the evaluated string.
---
 .../jdbc/postgis/PostGISFilterToSQL.java      | 11 ++++++---
 .../jdbc/oracle/OracleFilterToSQL.java        | 11 ++++++---
 2 files changed, 16 insertions(+), 6 deletions(-)

diff --git a/modules/plugin/jdbc/jdbc-postgis/src/main/java/org/geotools/data/postgis/PostGISFilterToSQL.java b/modules/plugin/jdbc/jdbc-postgis/src/main/java/org/geotools/data/postgis/PostGISFilterToSQL.java
index 73b4d8a1..9b1c2a34 100644
--- a/modules/plugin/jdbc/jdbc-postgis/src/main/java/org/geotools/data/postgis/PostGISFilterToSQL.java
+++ b/modules/plugin/jdbc/jdbc-postgis/src/main/java/org/geotools/data/postgis/PostGISFilterToSQL.java
@@ -150,9 +150,13 @@ public class PostGISFilterToSQL extends BasicFilterToSQL {
             Expression jsonColumn = function.getParameters().get(0);
             Expression value = function.getParameters().get(1);
 
             jsonColumn.accept(this, context);
             out.write(" @> ");
-            out.write("'" + value.evaluate(null, String.class) + "'::jsonb");
+            
+            String strValue = value.evaluate(null, String.class);
+            if (strValue != null) {
+                // Mitigate SQL Injection by properly escaping literal strings
+                out.write("'" + escapeLiteral(strValue) + "'::jsonb");
+            } else {
+                out.write("NULL");
+            }
             return;
         }
 
diff --git a/modules/plugin/jdbc/jdbc-oracle/src/main/java/org/geotools/data/oracle/OracleFilterToSQL.java b/modules/plugin/jdbc/jdbc-oracle/src/main/java/org/geotools/data/oracle/OracleFilterToSQL.java
index 19d8a3b1..44c9b2d8 100644
--- a/modules/plugin/jdbc/jdbc-oracle/src/main/java/org/geotools/data/oracle/OracleFilterToSQL.java
+++ b/modules/plugin/jdbc/jdbc-oracle/src/main/java/org/geotools/data/oracle/OracleFilterToSQL.java
@@ -205,9 +205,13 @@ public class OracleFilterToSQL extends BasicFilterToSQL {
             Expression jsonColumn = function.getParameters().get(0);
             Expression value = function.getParameters().get(1);
 
             out.write("JSON_EXISTS(");
             jsonColumn.accept(this, context);
-            out.write(", '$?(@ == ''" + value.evaluate(null, String.class) + "'')')");
+            
+            String strValue = value.evaluate(null, String.class);
+            if (strValue != null) {
+                // Mitigate SQL Injection by properly escaping literal strings
+                out.write(", '$?(@ == ''" + escapeLiteral(strValue) + "'')')");
+            } else {
+                out.write(", '$?(@ == null)')");
+            }
             return;
         }
-- 
2.39.2

```
